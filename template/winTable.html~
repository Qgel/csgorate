<!doctype html>

<html lang="en">
<head>
  <meta charset="utf-8">
  
  <title> CS:GO Win Table for Map: {{ curMap }} </title>
  <meta name="description" content="Win Chances for CounterStrike: Global Offensive competetive matches based on previous games">
  <meta name="author" content="qgel">

  <link rel="stylesheet" href="style.css">

  <!--[if lt IE 9]>
  <script src="http://html5shiv.googlecode.com/svn/trunk/html5.js"></script>
  <![endif]-->
</head>

<body>
  <div id="nav">
  <ul>
    <li> <a href="index.html">Home</a> </li>
    <li> Map
      <ul> 
      {% for map in maps %}
        <li> <a href="{{ map }}.html">{{ map }}</a> </li>
      {% endfor %} 
      </ul>
    </li>
    <li> Matches
      <ul> 
        <li> 200 </li>
        <li> 500 </li>
        <li> 1000 </li>
      </ul>
    </li>
  </ul>
  </div>

  <div id="info"> 
    Ranking of CS:GO teams based on the latest {{ numGames }} Games on map '{{ curMap }}' from hltv.org as of {{ gentime }}. <br>
    <b>Prediction Table</b>:<br> This Table shows a prediction of win chance against each other team.<br> 
    For example, we predict {{ example.winner }} has a chance of {{ example.chance }}% to win against {{ example.loser }}.
  </div>

  <table id="winTable">
  {% for column in winTable %}
    <tr>
    {% for entry in column %}
      <th>{{ entry }}</th>
    {% endfor %}
    </tr>
  {% endfor %}
  </table>

</body>
</html>
